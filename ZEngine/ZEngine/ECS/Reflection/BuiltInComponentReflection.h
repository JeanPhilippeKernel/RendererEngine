#pragma once

namespace ZEngine::ECS::Components
{
    // Registers reflection metadata for every built-in component.
    void RegisterBuiltInComponentReflection();
} // namespace ZEngine::ECS::Components
