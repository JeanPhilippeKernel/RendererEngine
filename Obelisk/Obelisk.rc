// Obelisk Windows application resource file
// Embeds the app icon into the PE executable so it appears
// in Explorer and the taskbar before the engine launches.

#include <windows.h>

IDI_APPICON ICON "../Resources/Editor/Settings/Icons/AppIconBadge.ico"
