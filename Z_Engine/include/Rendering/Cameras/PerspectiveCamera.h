#pragma once
#include "Camera.h"

namespace Z_Engine::Rendering::Cameras {
	
	class PerspectiveCamera : public Camera {
	public:
		PerspectiveCamera() = default;
		~PerspectiveCamera() = default;

	};
}
